{
    "version": "2.0.0",
    "name": "Aggressive Policy",
    "rules": [
        {
            "condition": "score > 0.4",
            "action": "BLOCK"
        },
        {
            "condition": "score > 0.2",
            "action": "ACCELERATE"
        }
    ]
}
