// adapters/dash.js
/**
 * 🎮 DASH / generic player adapter.
 */
const DashAdapter = {
    // Logic for DASH players
};

if (typeof window !== 'undefined') {
    window.Adapters = window.Adapters || {};
    window.Adapters.Dash = DashAdapter;
}
